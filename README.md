# inventory-automation
This project supports both local and remote inventory collection using CimSession. Remote inventory requires WinRM to be enabled and properly configured, along with sufficient permissions on the target machines.
